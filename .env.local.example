# Zoho Mail SMTP Settings
ZOHO_USER=you@yourdomain.com
ZOHO_PASS=your_zoho_app_password

# Optional: default sender name
SENDER_NAME=Mike Atolagbe

# Optional: lock down access with a simple API key
# Generate any random string, e.g: openssl rand -hex 32
APP_SECRET=your_secret_key_here
